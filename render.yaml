services:
  - type: web
    name: vulnerabily-scanner
    env: python
    buildCommand: |
      apt-get install -y google-chrome-stable
      pip install -r requirements.txt
    startCommand: python app.py